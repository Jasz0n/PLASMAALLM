# Mid-size HF student for LoRA ablation (CPU/GPU).
# Requires: pip install -e ".[ml]"
name: student-1.5b
provider: huggingface
model_id: Qwen/Qwen2.5-1.5B-Instruct
device: auto
dtype: auto
generation:
  max_new_tokens: 256
  temperature: 0.0
  top_p: 1.0
